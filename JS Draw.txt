javascript:(()=>{if(window.__drawApp)return;window.__drawApp=1;const d=document;const wrap=d.createElement('div');wrap.style='position:fixed;inset:0;z-index:999999;background:#111;display:flex;flex-direction:column;font-family:sans-serif';const bar=d.createElement('div');bar.style='background:#222;color:#fff;padding:8px;display:flex;gap:8px;align-items:center;flex-wrap:wrap';const canvas=d.createElement('canvas');canvas.style='flex:1;background:white;cursor:crosshair';wrap.append(bar,canvas);d.body.appendChild(wrap);const ctx=canvas.getContext('2d');function resize(){canvas.width=innerWidth;canvas.height=innerHeight-50;}resize();addEventListener('resize',resize);let tool='pen',drawing=false,sx=0,sy=0,color='#000000',size=3,snap=null;function btn(t,f){const b=d.createElement('button');b.textContent=t;b.style='padding:6px 10px;border:none;background:#444;color:#fff;border-radius:6px;cursor:pointer';b.onclick=f;bar.appendChild(b);return b;}[['Pen','pen'],['Line','line'],['Rect','rect'],['Circle','circle'],['Eraser','eraser']].forEach(([label,val])=>btn(label,()=>tool=val));const col=d.createElement('input');col.type='color';col.value=color;col.oninput=e=>color=e.target.value;bar.appendChild(col);const rng=d.createElement('input');rng.type='range';rng.min=1;rng.max=50;rng.value=size;rng.oninput=e=>size=e.target.value;bar.appendChild(rng);btn('Save PNG',()=>{const a=d.createElement('a');a.download='drawing.png';a.href=canvas.toDataURL();a.click();});btn('Clear',()=>{ctx.clearRect(0,0,canvas.width,canvas.height);});btn('Close',()=>{wrap.remove();delete window.__drawApp;});function pos(e){const r=canvas.getBoundingClientRect();return{x:e.clientX-r.left,y:e.clientY-r.top};}canvas.onmousedown=e=>{drawing=true;const p=pos(e);sx=p.x;sy=p.y;if(tool==='pen'||tool==='eraser'){ctx.beginPath();ctx.moveTo(sx,sy);}snap=ctx.getImageData(0,0,canvas.width,canvas.height);};canvas.onmousemove=e=>{if(!drawing)return;const p=pos(e);ctx.lineWidth=size;ctx.lineCap='round';ctx.strokeStyle=tool==='eraser'?'#fff':color;if(tool==='pen'||tool==='eraser'){ctx.lineTo(p.x,p.y);ctx.stroke();return;}ctx.putImageData(snap,0,0);if(tool==='line'){ctx.beginPath();ctx.moveTo(sx,sy);ctx.lineTo(p.x,p.y);ctx.stroke();}if(tool==='rect'){ctx.strokeRect(sx,sy,p.x-sx,p.y-sy);}if(tool==='circle'){ctx.beginPath();const r=Math.hypot(p.x-sx,p.y-sy);ctx.arc(sx,sy,r,0,Math.PI*2);ctx.stroke();}};canvas.onmouseup=()=>drawing=false;canvas.onmouseleave=()=>drawing=false;})();
