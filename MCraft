javascript:(()=>{const s=document.createElement('script');s.src='https://cdn.jsdelivr.net/npm/three@0.160.0/build/three.min.js';s.onload=()=>{function d(h,a){const c=new THREE.Color(h);c.offsetHSL(0,-a,0);return c}const sky=d(0x87ceeb,.2),wrap=document.createElement('div');wrap.style='position:fixed;inset:0;z-index:999999;background:#87ceeb';document.body.appendChild(wrap);const scene=new THREE.Scene();scene.fog=new THREE.Fog(sky,60,220);scene.background=sky;const camera=new THREE.PerspectiveCamera(75,innerWidth/innerHeight,.1,1000),renderer=new THREE.WebGLRenderer({antialias:false});renderer.setPixelRatio(1);renderer.setSize(innerWidth,innerHeight);renderer.setClearColor(sky,1);wrap.appendChild(renderer.domElement);scene.add(new THREE.AmbientLight(0xffffff,.9));const sun=new THREE.DirectionalLight(0xffffff,.7);sun.position.set(50,100,30);scene.add(sun);const cloudGeo=new THREE.BoxGeometry(10,3,10),cloudMat=new THREE.MeshBasicMaterial({color:0xffffff,transparent:true,opacity:.8,depthWrite:false}),clouds=new THREE.Group();scene.add(clouds);function makeCloud(x,y,z,s=6){const g=new THREE.Group();for(let i=0;i<5+Math.random()*8;i++){const cube=new THREE.Mesh(cloudGeo,cloudMat);cube.position.set(x+(Math.random()-.5)*s*12,y+(Math.random()-.5)*3,z+(Math.random()-.5)*s*12);cube.scale.setScalar(1.5+Math.random()*1.5);g.add(cube)}clouds.add(g)}for(let i=0;i<60;i++)makeCloud((Math.random()-.5)*800,45+Math.random()*30,(Math.random()-.5)*800);const geo=new THREE.BoxGeometry(1,1,1),mats={grass:new THREE.MeshLambertMaterial({color:d(0x5fb14a,.2)}),dirt:new THREE.MeshLambertMaterial({color:d(0x8a5a2b,.2)}),stone:new THREE.MeshLambertMaterial({color:d(0x888888,.2)}),sand:new THREE.MeshLambertMaterial({color:d(0xd9c27a,.2)}),water:new THREE.MeshLambertMaterial({color:d(0x3399ff,.2),transparent:true,opacity:.7})},CHUNK=16,RENDER=4,WATER=9,chunks=new Map();function noise(x,z){return(Math.sin(x*.04)+Math.cos(z*.05))*4+Math.sin((x+z)*.025)*6}function key(x,z){return x+','+z}function inst(list,mat,group){if(!list.length)return;const mesh=new THREE.InstancedMesh(geo,mats[mat],list.length),m=new THREE.Matrix4();for(let i=0;i<list.length;i++){const p=list[i];m.setPosition(p[0],p[1],p[2]);mesh.setMatrixAt(i,m)}group.add(mesh)}function build(cx,cz){const k=key(cx,cz);if(chunks.has(k))return;const group=new THREE.Group(),blocks={grass:[],dirt:[],stone:[],sand:[],water:[]};for(let x=0;x<CHUNK;x++)for(let z=0;z<CHUNK;z++){const wx=cx*CHUNK+x,wz=cz*CHUNK+z;let h=Math.floor(noise(wx,wz)*.5+10);h=Math.max(2,Math.min(18,h));for(let y=0;y<h;y++){let t=y===h-1?(h<=WATER?'sand':'grass'):y<h-3?'stone':'dirt';blocks[t].push([wx,y,wz])}if(h<WATER)blocks.water.push([wx,WATER-1,wz])}for(const t in blocks)inst(blocks[t],t,group);for(let i=0;i<2;i++){const tx=cx*CHUNK+Math.random()*CHUNK,tz=cz*CHUNK+Math.random()*CHUNK;let h=Math.floor(noise(tx,tz)*.5+10);if(h>=WATER+1){const trunk=new THREE.Mesh(new THREE.BoxGeometry(1,3,1),new THREE.MeshLambertMaterial({color:d(0x6b4423,.2)}));trunk.position.set(tx,h+1,tz);group.add(trunk);const leaves=new THREE.Mesh(new THREE.BoxGeometry(3,3,3),new THREE.MeshLambertMaterial({color:d(0x2f8f2f,.2)}));leaves.position.set(tx,h+4,tz);group.add(leaves)}}scene.add(group);chunks.set(k,group)}function update(){const pcx=Math.floor(camera.position.x/CHUNK),pcz=Math.floor(camera.position.z/CHUNK);for(let x=-RENDER;x<=RENDER;x++)for(let z=-RENDER;z<=RENDER;z++)build(pcx+x,pcz+z);for(const[k,obj]of chunks){const[cx,cz]=k.split(',').map(Number);if(Math.abs(cx-pcx)>RENDER+1||Math.abs(cz-pcz)>RENDER+1){scene.remove(obj);chunks.delete(k)}}}camera.position.set(0,18,25);let yaw=0,pitch=0,velY=0,onGround=false;const keys={};onkeydown=e=>keys[e.key.toLowerCase()]=1;onkeyup=e=>keys[e.key.toLowerCase()]=0;renderer.domElement.onclick=()=>renderer.domElement.requestPointerLock();onmousemove=e=>{if(document.pointerLockElement===renderer.domElement){yaw-=e.movementX*.002;pitch-=e.movementY*.002;pitch=Math.max(-1.4,Math.min(1.4,pitch))}};function ground(x,z){let h=Math.floor(noise(x,z)*.5+10);h=Math.max(2,Math.min(18,h));return h+2}function move(){const s=.18;if(keys.w){camera.position.x-=Math.sin(yaw)*s;camera.position.z-=Math.cos(yaw)*s}if(keys.s){camera.position.x+=Math.sin(yaw)*s;camera.position.z+=Math.cos(yaw)*s}if(keys.a){camera.position.x-=Math.cos(yaw)*s;camera.position.z+=Math.sin(yaw)*s}if(keys.d){camera.position.x+=Math.cos(yaw)*s;camera.position.z-=Math.sin(yaw)*s}const floor=ground(camera.position.x,camera.position.z);if(keys[' ']&&onGround){velY=.35;onGround=false}velY-=.02;camera.position.y+=velY;if(camera.position.y<=floor){camera.position.y=floor;velY=0;onGround=true}}function loop(){requestAnimationFrame(loop);camera.rotation.order='YXZ';camera.rotation.y=yaw;camera.rotation.x=pitch;move();update();clouds.position.x+=.01;renderer.render(scene,camera)}update();loop();addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)})};document.body.appendChild(s)})();
